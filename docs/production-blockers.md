# Production Blockers

Severity:

- P0 = cannot deploy
- P1 = must fix before launch
- P2 = should fix soon
- P3 = improvement

| Blocker | Severity | Affected module | Reproduction steps | Fix status | Owner | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| Live Frappe bench unavailable in this workspace | P0 | Frappe migration and persistence validation | Start the pinned Docker stack and run `bench --site lebtech.local list-apps` | Fixed | Platform/DevOps | A Dockerized Frappe/ERPNext bench is running with MariaDB/Redis and site `lebtech.local`. Local host still does not have the native `bench` CLI; use Docker bench commands from `docs/live-bench-runbook.md`. |
| Frappe API credentials missing | P0 | `npm run smoke:frappe` | Run `npm run smoke:frappe` with `.env.local` Frappe values loaded | Fixed | Platform/DevOps | Dedicated local API user `portal-api@lebtech.local` was created and `.env.local` was populated. Secrets remain local and ignored. |
| Live DocType migration not executed | P0 | Frappe DocTypes | Run `docker compose run --rm --no-deps backend bench --site lebtech.local migrate` | Fixed | Platform/DevOps | Live migration passed after fixing app module layout, `PNL Snapshot` naming, fixtures, queue status options, and Docker Redis/site routing. |
| Live persistence smoke not executed | P0 | Leads, invoices, receipts, commissions, delete queue, audit, API keys | Run `PLATFORM_BASE_URL=http://localhost:3001 npm run smoke:frappe` with live Frappe credentials | Fixed | Platform/DevOps | Live smoke passed against Frappe and the Next boundary returned `source: frappe`. |
| Docker build initially copied host artifacts into Linux image | P1 | Container deployment | Run `docker compose build` before `.dockerignore` fix | Fixed | Platform/DevOps | Added `.dockerignore` and explicit Linux native optional packages for Tailwind/Lightning CSS. `docker compose build --no-cache frontend` and `docker compose build` now pass. |
| ERPNext image tag is not pinned | P1 | Docker/Frappe deployment | Inspect `ERPNEXT_IMAGE` default in `.env.example` / `docker-compose.yml` | Fixed | Platform/DevOps | Pinned ERPNext `15.111.0` / Frappe `15.110.0` by manifest digest. MariaDB `10.6.27`, Redis, and NGINX are also pinned. |
| Live Frappe permission matrix not validated | P1 | Frappe permissions | Run focused Super Admin, Regional Director, Reseller Admin, and Sales Team User checks on `lebtech.local` | Fixed | Backend/Security | Live matrix passed after tightening `Portal API Key`, `Global Portal Setting`, API-key methods, and assignment-aware permission hooks. |
| Frappe backend used development server | P1 | Frappe web runtime | Inspect backend logs for Werkzeug development server warning | Fixed | Platform/DevOps | Backend now installs the mounted app and starts the image-provided Gunicorn entrypoint with configurable workers, threads, and timeout. |
| Worker, scheduler, and backup execution unvalidated | P1 | Operations | Run `npm run smoke:frappe:operations` | Fixed | Platform/DevOps | Active scheduler, two online workers, real short-queue execution, and database/public/private backup artifacts passed. |
| Backup restore drill not executed | P1 | Disaster recovery | Run `npm run smoke:frappe:restore` | Fixed | Platform/DevOps | Latest database and file backup restored into an isolated temporary site, migrated, core records validated, and temporary site removed. |
| Generic Frappe proxy and public backend binding | P1 | Edge security | Request `/erpnext-api/api/method/ping` and inspect the backend published address | Fixed | Platform/DevOps | Generic Frappe proxy now returns `404`; the backend host port binds to `127.0.0.1` only. |
| Dependency-aware health and edge diagnostics missing | P1 | Operations | Request `/api/health/live` and `/api/health/ready`; inspect NGINX response headers/logs | Fixed | Platform/DevOps | Added authenticated Frappe readiness, request IDs, JSON access logs, rate limiting, and edge smoke coverage. |
| Production domain and TLS not provisioned | P1 | Public ingress | Resolve the production hostname and run `EDGE_BASE_URL=https://<domain> npm run smoke:edge` | Open, execution-ready | Platform/DevOps | DNS/TLS procedure and production NGINX template exist. Requires production DNS and certificate authority access; HTTP-only local validation cannot close this gate. |
| Production secrets not stored and rotated in a managed secret store | P1 | Credentials | Run `npm run preflight:production` using the production secret injection path | Open, execution-ready | Security/DevOps | Secret-file runtime support, environment contract, validation, rotation, and revocation procedures exist. A real production secret store and rotated credentials are still required. |
| Backup bundle not transferred to encrypted off-host storage | P1 | Disaster recovery | Run `npm run backup:offhost`, retrieve the bundle, and run decrypt verification | Open, locally proven | Platform/DevOps | AES-256-GCM encryption, filesystem transfer, checksum, decrypt, and plaintext verification passed locally. A real S3/R2/SFTP target and retention policy evidence are still required. |
| External monitoring and alert delivery not connected | P1 | Operations | Run `npm run monitor:probe`, trigger a readiness failure, and confirm an alert reaches the on-call destination | Open, execution-ready | Platform/DevOps | Aggregate health and host probe cover services, jobs, queues, backups, disk, and TLS. A monitoring provider and delivered test alert are still required. |
| Production ingress/WAF policy not activated | P1 | Edge security | Apply `deploy/nginx/production.conf.example`, enable WAF rules, and capture blocked-path events | Open, execution-ready | Security/DevOps | Origin and Cloudflare/WAF rules are documented. Local NGINX edge passes; provider-side WAF activation needs production proof. |
| Scheduled WhatsApp reminders failed without provider configuration | P1 | Scheduler/integrations | Run the hourly reminder scheduler with no WhatsApp token configured and inspect failed RQ jobs | Fixed | Backend | Provider selection now reads Integration Setting/site config and unconfigured reminders return a non-failing skipped result. Existing resolved failures were removed after validation. |
| Permission smoke generated contact-job cleanup races | P2 | Validation/operations | Run permission smoke and inspect failed `create_contact` RQ jobs | Fixed | Backend/Security | Test contacts are now created synchronously and cleaned before users. A post-smoke failed-job summary remains zero. |
| Protected UI routes render without authentication | P0 | Portal routing | Open API keys, impersonation, delete queue, roles, or audit without a portal session | Fixed | Frontend/Security | Server-side route authorization passed anonymous, role-denial, true-Super-Admin, and impersonation regression tests. Sensitive controls are absent from denied responses. |
| Invalid financial detail URLs show unrelated records | P1 | Invoice/receipt routing | Open a nonexistent invoice or receipt ID | Fixed | Frontend | Missing invoice and receipt IDs render entity-specific not-found states and never substitute another record. |
| Operational UI split between dev-store and static data | P1 | Portal data loading | Run with Frappe configured and inspect dashboard/core list source | Fixed | Frontend/Backend | Dashboard leads and core customer, invoice, receipt, commission, and report views resolve through the Frappe backend adapter and display `source: frappe`. Configured Frappe failures do not silently fall back. |
| Leads navigation incomplete | P1 | Leads workspace | Open `/leads` and inspect the sidebar target | Fixed | Frontend | The scoped, filterable Leads workspace loads live Frappe records and both navigation shells target `/leads`. |
| Navigation inconsistent and hides functionality | P2 | Portal navigation | Compare operational routes with visible navigation | Fixed | Frontend | Grouped, role-filtered Main, Accounting, Integrations, and Admin navigation exposes the operational route set. |
| Customers incorrectly highlights Settings | P2 | Portal navigation | Open `/customers` | Fixed | Frontend | Active state derives from `usePathname`; browser and HTTP tests confirm Customers alone receives `aria-current="page"`. |
| Compact navigation lacks affordance | P2 | Responsive navigation | Open an operational page at 390px | Fixed | Frontend | A visible Menu button opens the complete role-filtered navigation at 390px and 704px; desktop navigation remains expanded at 1440px. |
| Exhibition slot holds are non-persistent (in-memory only) | P1 | Exhibition slots (APP-10) | Place a booth hold via `POST /api/slots/hold`, restart the app, and re-read slot status | Open, accepted limitation | Backend/Frontend | No Frappe DocType exists for slots, so config/layout/holds live only in the in-memory dev-store — lost on restart and inconsistent across instances. Documented and made explicit (`docs/exhibition-slots-persistence.md`, code comments); slot write routes are intentionally exempt from the fail-loud gate. Durable persistence (add a `Slot Hold` DocType + wire through `maybeRouteToFrappe`) is a tracked follow-up. |

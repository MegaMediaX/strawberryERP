import { SalesPlaceholder } from "@/components/sales/SalesPlaceholder";

export default function SalesProfilePage() {
  return <SalesPlaceholder title="Profile" detail="Appearance, calendar, notification preferences, security (spec §22)." />;
}

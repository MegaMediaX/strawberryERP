import { AdminPlaceholder } from "@/components/admin/AdminPlaceholder";

export default function Page() {
  return <AdminPlaceholder title="Resellers" detail="All resellers across the platform, with country badges + Login As (§10)." />;
}

import { AdminPlaceholder } from "@/components/admin/AdminPlaceholder";

export default function Page() {
  return <AdminPlaceholder title="Leads" detail="All leads globally, full admin actions + ownership (§13)." />;
}

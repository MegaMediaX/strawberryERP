import { AdminPlaceholder } from "@/components/admin/AdminPlaceholder";

export default function Page() {
  return <AdminPlaceholder title="Invoicing" detail="Invoice numbering + PDF/QR/share settings, live preview (§18)." />;
}

import { AdminPlaceholder } from "@/components/admin/AdminPlaceholder";

export default function Page() {
  return <AdminPlaceholder title="Countries" detail="Manage business regions — currency, timezone, invoice prefix, branding (§9)." />;
}

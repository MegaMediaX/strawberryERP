import { AdminPlaceholder } from "@/components/admin/AdminPlaceholder";

export default function Page() {
  return <AdminPlaceholder title="API Keys" detail="Generate API keys (read/create/update only — no delete) (§23)." />;
}
